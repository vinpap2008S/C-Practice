#include <iostream>
#include <list>
#include <string>

using namespace std;
/*
int main() {
    setlocale(LC_ALL, "ru");
    list<string> wordsOrNumbers;

    string input;
    cout << "Введите слова или числа (введите точку для завершения): ";
    while (cin >> input and input != ".")
    {
        wordsOrNumbers.push_back(input);
    }

    cout << "Список значений до точки" << endl;
    for (const string& item : wordsOrNumbers) {
        cout << item << endl;
    }


    return 0;
}
*/